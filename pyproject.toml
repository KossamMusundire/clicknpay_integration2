
[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "clicknpay_integration"
version = "1.0.0"
description = "Replace Paynow with ClicknPay"
dependencies = ["requests"]
